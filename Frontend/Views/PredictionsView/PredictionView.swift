import SwiftUI

struct PredictionView: View {
    var body: some View {
        NavigationView {
            // Not done yet...
        }
    }
}

// MARK: - Preview Provider
struct PredictionView_Preview: PreviewProvider {
    static var previews: some View {
        PredictionView()
    }
}
